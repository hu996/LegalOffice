@model IEnumerable<LegalOffice.Domain.Entities.Contract>
@{
    ViewData["Title"] = "العقود";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h3 class="m-0">العقود</h3>
    <a asp-action="Create" class="btn btn-primary">إضافة عقد</a>
</div>

<form method="get" class="mb-3">
    <div class="row g-2">
        <div class="col-md-10">
            <input name="search" value="@ViewBag.Search" class="form-control" placeholder="بحث برقم العقد أو العنوان أو اسم العميل" />
        </div>
        <div class="col-md-2">
            <button class="btn btn-outline-primary w-100" type="submit">بحث</button>
        </div>
    </div>
</form>

<div class="table-responsive">
    <table class="table table-striped align-middle">
        <thead>
            <tr>
                <th>رقم العقد</th>
                <th>العنوان</th>
                <th>العميل</th>
                <th>النوع</th>
                <th>الحالة</th>
                <th>القيمة</th>
                <th>البداية</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ContractNumber</td>
                <td>@item.Title</td>
                <td>@item.Client?.FullName</td>
                <td>@item.ContractTypeLookup?.NameAr</td>
                <td>@item.StatusLookup?.NameAr</td>
                <td>@item.ContractValue</td>
                <td>@item.StartDate.ToString("yyyy-MM-dd")</td>
                <td class="text-end">
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">تعديل</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>
